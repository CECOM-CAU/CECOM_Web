import NavBarDesktop from "@/app/_components/NavBar/NavBarDesktop";

export const NavBar = () => {
    return (
        <div className="w-full flex flex-row fixed top-0 z-20">
            <NavBarDesktop/>
        </div>
    )
}